meta {
  name: 05 Get Account
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: none
}

tests {
  test("renvoie 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("renvoie le compte connecte", function () {
    expect(res.getBody().email).to.equal(bru.getVar("testEmail"));
  });
}
