meta {
  name: 07 Register Gmail Rejected
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "test.gmail.reject@gmail.com",
    "displayName": "Test Gmail",
    "password": "motdepasse123"
  }
}

tests {
  test("renvoie 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
