meta {
  name: 05 Logout
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: none
}

tests {
  test("renvoie 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
