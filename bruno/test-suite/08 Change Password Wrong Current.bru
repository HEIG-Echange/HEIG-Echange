meta {
  name: 08 Change Password Wrong Current
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/auth/me
  body: json
  auth: none
}

body:json {
  {
    "currentPassword": "mauvaismotdepasse",
    "password": "nouveaumotdepasse123"
  }
}

tests {
  test("renvoie 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
