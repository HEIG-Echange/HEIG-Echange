meta {
  name: 02 Login Wrong Password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testEmail}}",
    "password": "mauvaismotdepasse"
  }
}

tests {
  test("renvoie 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
