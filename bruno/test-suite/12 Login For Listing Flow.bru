meta {
  name: 12 Login For Listing Flow
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testEmail}}",
    "password": "{{testPassword}}"
  }
}

tests {
  test("renvoie 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
