meta {
  name: 09 Change Password Wrong Current
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/auth/me
  body: json
  auth: none
}

body:json {
  {
    "currentPassword": "mauvaismotdepasse",
    "password": "nouveaumotdepasse123"
  }
}

tests {
  test("renvoie 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
