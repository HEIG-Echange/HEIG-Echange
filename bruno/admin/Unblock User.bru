meta {
  name: Unblock User
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/users/{{userId}}/unblock
  body: json
  auth: none
}

body:json {
  {
    "reason": "{{unblockReason}}"
  }
}

vars:pre-request {
  userId: 1
  unblockReason: Verifie, compte reactive
}
