meta {
  name: Verify Email
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/verify-email
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "code": "{{devVerificationCode}}"
  }
}
