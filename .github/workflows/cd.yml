name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7

      # TODO build image docker -> push ghcr.io -> deploy hook Render
      - run: echo "TODO deploy"
